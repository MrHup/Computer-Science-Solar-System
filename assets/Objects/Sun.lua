 -- this is the big block that contains in its circle the other big domains
 
 Sun = Core.class(Sprite)
 
 function Sun:init()
	-- contains N slots for planets
	-- reads the data to initialize the planets
	self.test = "unu"
 end